# legacy/

Ancien prototype « The Apprentice » (simulation professionnelle), conservé pour référence
et exclu du build (`tsconfig.json` + `eslint.config.mjs`). Il peut être supprimé sans impact
sur EscapeClass.
